<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="app.lovable.75507a907e2b421c9e2d6aa7effd7c93">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:exported="true"
            android:label="@string/title_activity_main"
            android:launchMode="singleTask"
            android:name="app.lovable.75507a907e2b421c9e2d6aa7effd7c93.MainActivity"
            android:theme="@style/AppTheme.NoActionBarLaunch">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              Custom scheme deep link: santiyem://payment-callback
              Uses android:host="payment-callback" for strict matching.
              This ensures ONLY santiyem://payment-callback?... is caught,
              not every santiyem:// URL.

              Test with adb:
                adb shell am start -a android.intent.action.VIEW \
                  -d "santiyem://payment-callback?status=success&message=Test"
              See android/DEEP_LINK_TESTING.md for full test matrix.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="santiyem" android:host="payment-callback" />
            </intent-filter>

            <!-- 
              Fallback custom-scheme filter for general santiyem:// links
              (kept for backward compatibility with other deep-link paths).
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="santiyem" />
            </intent-filter>

            <!--
              Android App Links (autoVerify) for https://santiyem.io/payment-callback
              Requires .well-known/assetlinks.json published on santiyem.io
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https"
                      android:host="santiyem.io"
                      android:pathPrefix="/payment-callback" />
            </intent-filter>

        </activity>
    </application>

    <!-- Push notifications (Capacitor PushNotifications plugin / FCM) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

</manifest>
